"""Composite: flatten the base + overlay layers into one preview render.

A preview/handoff intermediate (``review/composite.mp4``), not the final cut —
the final master is yours, exported from your NLE into ``render/``. The composite
is the in-app "see the assembled result" preview and the muted guide track the
editor handoffs carry on top (SADD §7).
"""
